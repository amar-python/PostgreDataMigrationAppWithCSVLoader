# terraform.tfvars.example - Staging
#
#   cp terraform.tfvars.example terraform.tfvars
#
# Staging terraform.tfvars is gitignored. NEVER commit secrets here.

location               = "australiaeast"
cost_center            = "te-platform"

# PG sizing
pg_version             = "16"
pg_sku                 = "GP_Standard_D2s_v3"
pg_storage_mb          = 32768

# Backups / HA
backup_retention_days  = 14
geo_redundant_backups  = false  # not usually needed for staging
ha_mode                = "Disabled"  # "ZoneRedundant" only if testing HA failover

# Networking
vnet_cidr              = "10.41.0.0/20"
cae_zone_redundant     = true
